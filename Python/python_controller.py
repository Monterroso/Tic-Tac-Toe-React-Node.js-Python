import sys 
import Game from game
import UserPlayer from user_player


"""Contains functions needed to run an entire game


""" 


#Be given a game json object, and player choice, set the player choice, then run the game
#It needs to return the game object as a json, all possible choices
#If the game ends, it just returns, need a way to check if the game itself has finished
#If it is finished, doesn't need to return all possible choices, can send another argument to just indicate it is done

#Arguments are game json argument, 