import sys 
import Game from game
import UserPlayer from user_player


"""Contains functions needed to run an entire game


""" 

#Store list for all active games? Give game id and you can access it?
#Function that starts the game
#Function that gets the current board state, the current player, and the list of possible states
#Function where you send in and integer corresponding to the board state from the previous function
#   Sets that as the player's action to be the integer chosen

#List of games and an id

#Function that creates a game, and sets it's id
    #Takes in number to win, x-dist and y-dist as sys arguments, creates two user_players to use, with id 1,2, 
    #Returns the id

#Start game function given an id

#Take turn function given a game id, gets information from the game